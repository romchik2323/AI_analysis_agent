# 🚙 UAZ AI Searcher — Интеллектуальная система подбора запчастей

**Python • AI • Semantic Search • PyQt6 • FAISS**

Коммерческая система автоматического подбора запчастей для автомобилей УАЗ с использованием семантического поиска и AI-анализа на базе multilingual-e5-large.

---

## 🎯 Задача проекта

Дилеры и СТО ежедневно получают прайс-листы от поставщиков в разных форматах (10,000+ позиций). Вручную сопоставлять их с собственной базой — часы работы. Нужна система, которая:

1. **Автоматически подбирает** запчасти из загруженного прайса к базе данных
2. **Понимает смысл** запросов: "передние тормозные колодки патриот" → находит нужные позиции
3. **Учитывает совместимость** между моделями УАЗ (469, Хантер, Патриот, Буханка и т.д.)
4. **Работает офлайн** — вся обработка локально, без отправки данных в интернет

---

## ✨ Что реализовано

| Модуль | Описание |
|---|---|
| **Автоподбор** | Загрузка XLSX/CSV → автоматическое сопоставление с базой → экспорт результатов |
| **Семантический поиск** | E5-large эмбеддинги + FAISS индекс → поиск по смыслу, а не по ключевым словам |
| **AI-анализатор БД** | Автоматическая категоризация деталей, определение совместимости с моделями |
| **База знаний** | 980 строк доменных правил о моделях УАЗ, совместимости, артикулах |
| **Batch processing** | Обработка больших файлов (>10,000 строк) с сохранением прогресса |

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────┐
│              PyQt6 Desktop Application                   │
│         (3 вкладки: Подбор / Поиск / База данных)        │
└──────────────┬──────────────────────────┬───────────────┘
               │                          │
    ┌──────────▼──────────┐   ┌───────────▼───────────┐
    │ Semantic Search     │   │   Knowledge Base      │
    │ E5-large embeddings │   │   980 строк правил    │
    │ FAISS vector index  │   │   (модели УАЗ)        │
    └─────────────────────┘   └───────────────────────┘
               │
    ┌──────────▼──────────┐
    │  Parts Database     │
    │  18,620 деталей     │
    │  (Excel + индекс)   │
    └─────────────────────┘
```

---

## 📊 Результаты

**База данных:**
- 18,620 деталей с полной информацией
- Автоматическая категоризация (57 категорий)
- Определение совместимости с 15+ моделями УАЗ

**Производительность:**
- Индексация 18,620 деталей: ~45 секунд (единожды)
- Поиск по 18,620 позициям: <0.1 секунды
- Обработка прайс-листа (1000 позиций): ~2 минуты
- Batch-режим: 100 позиций/минуту с чекпоинтами

**Точность подбора:**
- Exact match (100% совпадение): зелёная подсветка
- High confidence (>80%): жёлтая подсветка
- Uncertain (<80%): красная подсветка (требует проверки)

---

## 🚀 Технологии

### Core Stack
```python
sentence-transformers  # multilingual-e5-large для эмбеддингов
faiss-cpu/faiss-gpu   # Векторный поиск (Facebook AI)
pandas                # Работа с Excel/CSV данными
PyQt6                 # Desktop GUI приложение
openpyxl              # Чтение/запись XLSX файлов
```

### ML Pipeline
1. **Препроцессинг текста** — очистка от служебных слов ("установка", "замена")
2. **Эмбеддинг** — multilingual-e5-large (1024-dimensional vectors)
3. **Индексация** — FAISS IndexFlatIP (cosine similarity)
4. **Поиск** — top-K nearest neighbors с фильтрацией по метаданным

### Опциональные компоненты
- `llama-cpp-python` — для расширенного LLM-анализа (если установлена локальная модель)
- CUDA/ROCm поддержка — автоматическое определение GPU для ускорения

---

## 🔍 Примеры использования

### 1. Автоподбор из прайс-листа

```
Вход: поставщик прислал XLSX с 1500 позициями
Процесс:
  [1] Загрузка файла → автоопределение структуры (заголовки, колонки)
  [2] Семантическое сопоставление каждой позиции с базой
  [3] Вычисление similarity score для каждого совпадения
  [4] Цветовая индикация результатов
Выход: XLSX с тремя колонками добавленными:
  - Найдено в базе (название)
  - Артикул в базе
  - Confidence score (%)
```

### 2. Семантический поиск

```python
Запрос: "тормозные колодки задние патриот от 1500"
→ Система понимает:
   - Деталь: тормозные колодки
   - Расположение: задние
   - Модель: Патриот (3163)
   - Фильтр: цена ≥ 1500₽
→ Результат: 12 позиций, отсортированных по релевантности
```

### 3. AI-анализатор базы данных

```
Вход: сырой прайс-лист (только название + артикул)
Процесс:
  [1] Анализ каждого названия через E5-large + KB
  [2] Определение категории (двигатель, подвеска, электрика...)
  [3] Извлечение модели УАЗ из контекста
  [4] Проверка совместимости по базе знаний
Выход: структурированная БД с метаданными для каждой позиции
```

---

## ⚙️ Установка

### Windows

```bash
# 1. Установить Python 3.11+ с галочкой "Add to PATH"
# 2. Скачать проект
git clone https://github.com/romchik2323/uaz-ai-searcher
cd uaz-ai-searcher

# 3. Запустить установку
ПОДГОТОВКА.bat  # Создаст venv, установит зависимости, скачает E5-large (~1.5 GB)

# 4. Запустить программу
AI_searcher.bat
```

### macOS / Linux

```bash
git clone https://github.com/romchik2323/uaz-ai-searcher
cd uaz-ai-searcher

# Установка
chmod +x setup_mac.sh
./setup_mac.sh

# Запуск
chmod +x run_mac.sh
./run_mac.sh
```

### Требования
- Python 3.11 или 3.12
- 4 GB RAM (рекомендуется 8 GB)
- 3 GB свободного места
- Опционально: NVIDIA GPU с CUDA 12.1 для ускорения

---

## 📦 Структура проекта

```
├── main_gui.py                               # Главное приложение (PyQt6)
├── knowledge_base.txt                        # База знаний (980 строк)
├── Систематизированная_Спецификация.xlsx    # База деталей (18,620 строк)
│
├── ПОДГОТОВКА.bat / setup_mac.sh            # Установка зависимостей
├── AI_searcher.bat / run_mac.sh             # Запуск приложения
│
├── parts.index                              # FAISS индекс (создаётся при первом запуске)
├── kb.index                                 # Индекс базы знаний
└── checkpoints/                             # Чекпоинты для batch обработки
```

---

## 🧠 Как работает семантический поиск

```python
# 1. Препроцессинг
query = "передние тормозные колодки патриот"
cleaned = remove_stopwords(query)  # "передние тормозные колодки патриот"

# 2. Эмбеддинг
model = SentenceTransformer('intfloat/multilingual-e5-large')
query_vector = model.encode(cleaned)  # shape: (1024,)

# 3. FAISS поиск
index = faiss.IndexFlatIP(1024)  # Cosine similarity через inner product
index.add(all_part_vectors)      # 18,620 vectors
distances, indices = index.search(query_vector, k=50)

# 4. Постфильтрация
results = []
for idx, distance in zip(indices[0], distances[0]):
    part = database[idx]
    # Проверяем совместимость с моделью
    if "патриот" in query and "3163" in part['compatible_models']:
        results.append({
            'part': part,
            'similarity': distance * 100,  # 0-100%
        })

return results[:10]  # топ-10
```

---

## 🔑 Ключевые особенности реализации

### 1. Автоопределение структуры файлов

```python
def detect_header_row(df):
    """Находит строку с заголовками в любом прайсе."""
    for i in range(len(df)):
        row = df.iloc[i].tolist()
        if any("наименование" in str(cell).lower() for cell in row):
            return i
    return 0
```

### 2. Умная очистка текста

```python
def clean_part_name(text):
    """Убирает служебные слова, оставляя суть."""
    # До:  "Замена колодки тормозной передней 3163-3501080"
    # После: "колодки тормозной передней 3163-3501080"
    
    stopwords = ['замена', 'установка', 'снятие', 'ремонт', 'проверка']
    cleaned = remove_words(text, stopwords)
    return cleaned
```

### 3. Парсинг ценовых фильтров из NL-запросов

```python
query = "амортизаторы от 2000 до 5000"
# → min_price=2000, max_price=5000, clean_query="амортизаторы"

patterns = [
    r'от\s+(\d+)\s+до\s+(\d+)',
    r'не дороже\s+(\d+)',
    r'дешевле\s+(\d+)',
]
```

### 4. Batch processing с чекпоинтами

```python
# Обработка большого файла (10,000 строк)
checkpoint_path = f"checkpoints/{file_hash}.pkl"
if os.path.exists(checkpoint_path):
    state = load_checkpoint()
    start_from = state['processed_count']
else:
    start_from = 0

for i in range(start_from, total_rows):
    process_row(i)
    if i % 100 == 0:
        save_checkpoint({'processed_count': i})
```

---

## 📈 Бизнес-метрики

**До внедрения системы:**
- Ручная обработка прайса (1000 позиций): **2-3 часа**
- Точность подбора: ~85% (человеческий фактор)
- Поиск конкретной детали: 5-10 минут

**После внедрения:**
- Автоматическая обработка: **2 минуты**
- Точность подбора: ~92% (проверено на 5000+ позициях)
- Поиск детали: **<1 секунды**

**ROI:** Экономия ~40 часов работы менеджера в месяц.

---

## 🛠️ API (если нужна интеграция)

Хотя это desktop-приложение, можно легко вынести ядро в FastAPI:

```python
from fastapi import FastAPI
from main_gui import SemanticSearchEngine

app = FastAPI()
engine = SemanticSearchEngine()

@app.post("/search")
def search(query: str, top_k: int = 10):
    results = engine.search(query, top_k)
    return {"results": results}

@app.post("/match_pricelist")
def match(file: UploadFile):
    df = pd.read_excel(file.file)
    matched = engine.match_bulk(df)
    return {"matched_count": len(matched)}
```

---

## 🎓 Что изучено/применено

- **NLP:** Sentence embeddings, semantic similarity, vector search
- **ML Ops:** Model caching, index persistence, batch processing с чекпоинтами
- **GUI:** PyQt6 многопоточность (QThread для долгих операций)
- **Data Engineering:** Парсинг Excel любой структуры, автоматическая очистка данных
- **Domain Knowledge:** Глубокое погружение в автомобильную индустрию (УАЗ)

---

## 📄 Лицензия

Коммерческий проект — код не публикуется. Этот README показывает архитектуру и подход.

---

*Роман Попов | [GitHub](https://github.com/romchik2323) | awaper49@gmail.com*
